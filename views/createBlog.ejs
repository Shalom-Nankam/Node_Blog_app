<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Create Blog</title>
    <style>
      body{
        background-color: bisque;
        margin: 50px 40px;
      }
      h2{
        text-align: center;
        font-size: xx-large;
        font-weight: bolder;
        margin-bottom: 50px;
      }
      .form-section{
        width: 100%;
        display: flex;
      }
      form{
        margin: auto;
      }
      .form-field{
        margin-bottom: 40px;
      }
      label{
        margin-right: 10px;
        font-size: 20px;
      }
      .btn-section{
        justify-content: center;
        display: flex;
      }
      #title, #tags{
        width: 745px;
      }

      #description{
        width: 690px;
      }
      

      input{
        outline: none;
        border: none;
        height: 35px;
        padding-inline: 10px;
      }

      textarea{
        height: 300px;
        width: 800px;
        padding: 10px;
        border: none;
        outline: none;
        margin-bottom: 50px;
      }

      #publish{
        height: 50px;
        width: 200px;
        border-radius: 10px;
        background-color: blue;
        color: white;
        margin-inline: 50px;
      }

      #draft{
        height: 50px;
        width: 200px;
        border-radius: 10px;
        background-color: white;
        color: black
      }

      .error {
        color: red;
        font-size: 16px;
        font-weight: bold;
        margin-top: 10px;
      }
      
    </style>
  </head>
  <body>
    <h2>Create blog</h3>
      <div class="form-section">
      <form action="/views/blogs/create" method="post">
        <div class="form-field">
          <label for="title">Title:</label>
          <input type="text" name="title" id="title" required maxlength="80"> <br>
        </div>
        <div class="form-field">
          <label for="description">Description:</label>
          <input type="text" name="description" id="description" required maxlength="100">
        </div>
        <div class="form-field">
          <label for="tags">Tags:</label>
          <input type="text" name="tags" id="tags" required maxlength="100" placeholder="#tag #tag #tag">
        </div>

        <textarea name="body" id="body" cols="30" rows="10" placeholder="Body..."></textarea>

        <input type="hidden" name="author" value="<%= user._id %>">


      <% if (error) { %>
        <p class="error"><%= error %></p>
        <% } %>

        <div class="btn-section">
          <input type="submit" value="Publish article" id="publish" name="state">
          <input type="submit" value="Save as draft" id="draft" name="state">
        </div>
        </form>
      </div>

  </body>
</html>
